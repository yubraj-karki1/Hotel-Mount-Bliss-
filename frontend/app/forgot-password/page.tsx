"use client";import Link from "next/link";import { useState,type FormEvent } from "react";import { ArrowLeft,Mail } from "lucide-react";import { authApi } from "@/services/auth-api";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input";
export default function ForgotPasswordPage(){const [email,setEmail]=useState("");const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{await authApi.forgotPassword(email);setSent(true)}catch{setError("Unable to submit the request. Please try again.")}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-background px-5"><section className="w-full max-w-md rounded-2xl border bg-card p-7 shadow-soft sm:p-9"><span className="grid size-12 place-items-center rounded-full bg-secondary text-primary"><Mail/></span><h1 className="mt-6 font-serif text-4xl text-primary">Reset your password</h1>{sent?<div className="mt-5"><p className="leading-7 text-muted-foreground">If an account exists for {email}, password-reset instructions have been sent.</p><Button asChild className="mt-7"><Link href="/login"><ArrowLeft/>Return to login</Link></Button></div>:<form className="mt-7 grid gap-5" onSubmit={submit}><label className="grid gap-2 text-sm font-semibold">Email address<Input type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label>{error&&<p className="text-sm text-destructive">{error}</p>}<Button disabled={busy} type="submit" size="lg">{busy?"Sending…":"Send reset link"}</Button><Link href="/login" className="text-center text-sm font-semibold text-primary hover:underline">Back to login</Link></form>}</section></main>}
