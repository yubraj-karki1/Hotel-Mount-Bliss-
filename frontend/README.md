# Hotel Mount Bliss frontend

Next.js frontend for the public hotel experience, customer booking tools, and role-based hotel administration dashboard.

Copy `.env.example` to `.env.local`, set `NEXT_PUBLIC_API_URL`, then use `npm run dev`. Run `npm run lint` and `npm run build` before deployment.
