"use client";import { useQuery } from "@tanstack/react-query";import { ConciergeBell } from "lucide-react";import { serviceApi } from "@/services/service-api";import { Card,CardContent } from "@/components/ui/card";import { formatCurrency } from "@/lib/utils";
export function PublicServices(){const q=useQuery({queryKey:["services"],queryFn:async()=> (await serviceApi.list()).data.data});if(q.isLoading)return <p className="mt-10 text-muted-foreground">Loading services…</p>;if(q.isError)return <p className="mt-10 text-destructive">Services are temporarily unavailable.</p>;return <div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{q.data?.map(s=><Card key={s._id}><CardContent className="p-6"><ConciergeBell className="size-6 text-accent"/><h2 className="mt-4 font-serif text-2xl text-primary">{s.name}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-muted-foreground">{s.description}</p><p className="mt-4 font-semibold">{formatCurrency(s.price)}</p></CardContent></Card>)}{q.data?.length===0&&<p className="text-muted-foreground">Service information will be available soon.</p>}</div>}
