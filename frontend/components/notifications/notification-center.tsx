"use client";
import { Bell, CheckCheck } from "lucide-react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { notificationApi } from "@/services/notification-api";
import { Button } from "@/components/ui/button";
import { Dialog,DialogContent,DialogDescription,DialogHeader,DialogTitle,DialogTrigger } from "@/components/ui/dialog";

export function NotificationCenter(){const client=useQueryClient();const q=useQuery({queryKey:["notifications"],queryFn:async()=>(await notificationApi.list()).data.data.items,refetchInterval:30_000});const refresh=()=>client.invalidateQueries({queryKey:["notifications"]});const read=useMutation({mutationFn:notificationApi.markRead,onSuccess:refresh});const all=useMutation({mutationFn:notificationApi.markAllRead,onSuccess:refresh});const items=q.data??[];const unread=items.filter(item=>!item.readAt).length;return <Dialog><DialogTrigger asChild><Button variant="ghost" size="icon" className="relative" aria-label={`Notifications${unread?`, ${unread} unread`:""}`}><Bell/>{unread>0&&<span className="absolute right-0 top-0 grid min-w-5 place-items-center rounded-full bg-destructive px-1 text-[10px] text-destructive-foreground">{unread>99?"99+":unread}</span>}</Button></DialogTrigger><DialogContent className="max-h-[80vh] overflow-y-auto sm:max-w-xl"><DialogHeader><DialogTitle>Notifications</DialogTitle><DialogDescription>Booking and hotel operation updates.</DialogDescription></DialogHeader><div className="flex justify-end"><Button size="sm" variant="ghost" disabled={!unread||all.isPending} onClick={()=>all.mutate()}><CheckCheck/>Mark all read</Button></div><div className="mt-2 grid gap-2">{q.isLoading&&<p className="p-4 text-sm text-muted-foreground">Loading notifications…</p>}{q.isError&&<p className="p-4 text-sm text-destructive">Notifications could not be loaded.</p>}{!q.isLoading&&!items.length&&<p className="p-4 text-sm text-muted-foreground">No notifications yet.</p>}{items.map(item=><button key={item._id} type="button" onClick={()=>!item.readAt&&read.mutate(item._id)} className={`rounded-lg border p-4 text-left ${item.readAt?"bg-background":"border-primary/25 bg-secondary/60"}`}><span className="flex items-start justify-between gap-4"><strong className="text-sm">{item.title}</strong>{!item.readAt&&<span className="mt-1 size-2 shrink-0 rounded-full bg-primary"/>}</span><span className="mt-1 block text-sm text-muted-foreground">{item.message}</span><span className="mt-2 block text-xs text-muted-foreground">{new Date(item.createdAt).toLocaleString()}</span></button>)}</div></DialogContent></Dialog>}
