"use client";import { Heart } from "lucide-react";import { apiClient } from "@/lib/api";import { Button } from "@/components/ui/button";import { toast } from "@/components/ui/toast";
export function FavoriteButton({roomId,onRemoved,remove=false}:{roomId:string;onRemoved?:()=>void;remove?:boolean}){async function click(){try{if(remove)await apiClient.delete(`/favorites/${roomId}`);else await apiClient.post(`/favorites/${roomId}`);toast.success(remove?"Removed from favorites":"Added to favorites");onRemoved?.()}catch{toast.error("Please sign in to manage favorites")}}return <Button type="button" onClick={()=>void click()} variant="secondary" size="icon" className="rounded-full" aria-label={remove?"Remove favorite":"Add favorite"}><Heart className={remove?"fill-current":""}/></Button>}
